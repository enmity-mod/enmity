import { applyTheme, getTheme, getThemeByName, listThemes, removeTheme, uninstallTheme } from '../utils/themes';

export {
  getTheme,
  getThemeByName,
  listThemes,
  applyTheme,
  removeTheme,
  uninstallTheme,
};
